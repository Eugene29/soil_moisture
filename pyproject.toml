[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "soil_moisture"
version = "0.1.0"
description = "Soil moisture modeling with Prithvi/TerraTorch"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["soil_moisture*"]